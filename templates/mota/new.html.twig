{% extends 'base.html.twig' %}

{% block title %}New Egoera{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="card border-light">
                <div class="card-header">
                    <h1>{{ "sidebar.mota" | trans }} {{ "berria" | trans }}</h1>
                </div>
                <div class="card-body text-right">
                    <a class="btn btn-outline-primary" href="{{ path('mota_index') }}"><i class="fas fa-arrow-circle-left"></i> {{ "Itzuli zerrendara" | trans }}</a>
                    <a class="btn btn-primary btnSave" href="javascript:void(0);"><i class="fas fa-save"></i> {{ "btnSave" | trans }}</a>
                </div>
            </div>
        </div>
    </div>

    {{ include('mota/_form.html.twig') }}

{% endblock %}
